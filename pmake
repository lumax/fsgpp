#usage : make all -f ProgMakefile
PROJECT_NAME=fsgProg
DEFS+=-D_GNU_SOURCE=1 -D_REENTRANT
INCLUDES+=-I$(INCLUDE_DIR)/libfsg
LIBS+=-L$(LIB_DIR)/


CFLAGS+=-g -Wall 
LDFLAGS+=-lfsg

OBJS =

include $(MAKE_DIR)/global.mak

